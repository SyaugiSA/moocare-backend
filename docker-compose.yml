services:
  web:
    build: .
    command: npm run start
    ports:
      - "8001:8001"
    environment:
      - DB=mongodb+srv://admin:admin@moocare.eq6n93z.mongodb.net/moocare
      - PORT=8001
      - SECRET=syaugisa
